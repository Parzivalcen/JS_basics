// A natural number is a "Doubleton Number", if it contains exactly two distinct digits. For example, 23, 35, 100, 12121 are doubleton numbers, and 123 and 114455 are not.

// Create a function which takes a number n and finds the next doubleton number.

function doubleton(num){
  // return the next number 
  // if number has 2 digits, return nice, else ignore
  // check if the numbers are distinc 
  // make it work with more than 2 digits
  let numStr = num.toString();
  let numLenght = numStr.length;
  for(let i = 10; i)

  // find if the numbers have two disting digits and return it 
  // if not ignore
}
console.log(doubleton(89))