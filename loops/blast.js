// loop from 10 to zero
for (let i = 10; i >= 0; i--){
 // if number is mores than zero print 'countdown n'
 if (i !== 0){
  console.log(`Countdown ${i}`)
  // if number == 0 print 'BlastOff'
 }else{
  console.log('Blast OFF!')}
  
  
}

// const para = document.createElement('p');
// para.textContent = ;
// output.appendChild(para);